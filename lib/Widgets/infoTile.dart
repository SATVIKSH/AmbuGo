import 'package:flutter/material.dart';
/*
* Created by Mujuzi Moses
*/

class InfoTiles extends StatelessWidget {
  const InfoTiles({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(),
    );
  }
}
